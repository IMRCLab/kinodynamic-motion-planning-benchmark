default:
  solver_id: 9
  linear_search: true # Binary search doesn't work for quadcopter models!
  smooth_traj: true

quad3d_v0:
  default:
    weight_goal: 400
    tsearch_max_rate: 10 # necessary because init guess is not reallistic!

quad2d_v0:
  default:
    weight_goal: 200
    tsearch_max_rate: 10 # necessary because init guess is not reallistic!

acrobot_v0:
  default:
    weight_goal: 400
    tsearch_max_rate: 10 # necessary because init guess is not reallistic!
    max_iter: 300
